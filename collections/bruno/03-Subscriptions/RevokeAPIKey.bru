meta {
  name: Revoke API Key
  type: http
  seq: 7
}

delete {
  url: {{subscription_url}}/api/v1/subscriptions/{{subscription_id}}/keys/{{api_key}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
