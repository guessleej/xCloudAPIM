meta {
  name: Publish Policy Chain
  type: http
  seq: 3
}

post {
  url: {{registry_url}}/api/v1/apis/{{api_id}}/policy-chain/publish
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status 200 or 202", function() {
    expect([200, 202]).to.include(res.status);
  });
}
